using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;


/// <summary>
/// A class that inherits <see cref="MonoBehaviour"/>.
/// </summary>
public class <+FILEBASE+> : MonoBehaviour
{
    /// <summary>
    /// This method is called only once during the lifetime of the script instance.
    /// </summary>
    private void Awake()
    {
    }

    /// <summary>
    /// This method is called on the frame when a script is enabled just before
    /// any of the Update methods are called the first time.
    /// </summary>
    private void Start()
    {
        <+CURSOR+>
    }

    /// <summary>
    /// This method is called every frame.
    /// </summary>
    private void Update()
    {
    }

    /// <summary>
    /// This method is called every fixed frame-rate frame.
    /// </summary>
    private void FixedUpdate()
    {
    }
}
